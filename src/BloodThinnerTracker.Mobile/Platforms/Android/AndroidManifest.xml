<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="${applicationId}">

  <!-- Permission required to receive BOOT_COMPLETED -->
  <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

  <application>
    <!-- Register the BootCompletedReceiver so we can reschedule jobs after reboot -->
    <receiver android:name="mono.android.RuntimeBroadcastReceiverProxy"
              android:enabled="true"
              android:exported="true">
      <!-- RuntimeBroadcastReceiverProxy is used by Xamarin/MAUI to route broadcasts to managed receivers -->
      <intent-filter>
        <action android:name="android.intent.action.BOOT_COMPLETED" />
        <action android:name="android.intent.action.LOCKED_BOOT_COMPLETED" />
      </intent-filter>
    </receiver>

    <!-- Note: The managed BootCompletedReceiver class will be invoked via the runtime proxy. -->
  </application>

</manifest>
