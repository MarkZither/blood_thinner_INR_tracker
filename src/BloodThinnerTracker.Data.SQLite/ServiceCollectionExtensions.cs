namespace BloodThinnerTracker.Data.SQLite
{
    /// <summary>
    /// Placeholder extension class created during a temporary refactor.
    /// Left intentionally empty to avoid introducing compile-time dependencies
    /// while changes are validated. Can be removed later.
    /// </summary>
    public static class ServiceCollectionExtensions
    {
        /// <summary>
        /// Stub extension created temporarily during provider registration refactor.
        /// Intentionally empty to avoid adding extra dependencies during build fixes.
        /// Remove when provider registration is finalized.
        /// </summary>
    }
}
