{
  "$schema": "https://json.schemastore.org/appsettings.json",
  "_comment": "For better security, use environment variables instead of this file",
  "_example": "Set POSTGRES_PASSWORD environment variable or use Parameters__postgres-password",
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "Features": {
    "EnableInfluxDB": false
  },
  "Parameters": {
    "postgres-password": "local_dev_only_password",
    "influxdb-password": "local_dev_only_password"
  }
}
